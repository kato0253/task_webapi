<h1>詳細画面</h1>
<p>お気に入り数: <%= @count %></p>
<% unless @article.user_id == current_user.id %>
<% if @favorite.present? %>
<%= link_to 'お気に入り解除する', favorite_path(id: @favorite.id), method: :delete, class: 'btn btn-danger' %>
<% else %>
<%= link_to 'お気に入りする', favorites_path(article_id: @article.id), method: :post, class: 'btn btn-primary' %>
<% end %>
<% end %>
<p>タイトル: <%= @article.title %></p>
<p>内容: <%= @article.content %></p>
<p>画像:<%= image_tag @article.image.url , :size => '100x100' unless @article.image.blank? %></p>
<p>コメント数: <%= @article.comments.size %></p>
<p>コメント一覧</p>
<div id="comments_area">
  <%= render partial: 'comments/index', locals: { comments: @comments, article: @article } %>
</div>
<!-- コメント入力欄をブログの詳細ページに表示するためのrender -->
<%= render partial: 'comments/form', locals: { comment: @comment, article: @article } %>
<%= link_to '編集', edit_article_path(@article) %> |
<%= link_to '戻る', articles_path %>
</div>
<a class="twitter-timeline" href="https://twitter.com/qYhei6zVyuMFVuX?ref_src=twsrc%5Etfw">Tweets by qYhei6zVyuMFVuX</a> <script async src="https://platform.twitter.com/widgets.js" charset="utf-8"></script>
