<ul>
  <% comments.each do |comment| %>
  <div>
    <div class="comment-content" id ="this_comment_<%= comment.id %>"></div>
    <p><%= comment.content %></p>
    <% if comment.id.present? && comment.user_id == current_user.id %>
    <p><%= link_to "コメント編集", edit_article_comment_path(article, comment), remote: true %></p>
    <p><%= link_to "コメント削除", article_comment_path(comment.article_id, comment.id), method: :delete, remote: true,
      data: { confirm: "本当に削除しますか?"} %></p>
  </div>
    <% end %>
   <% end %>
  </ul>
